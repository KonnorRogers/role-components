/**
 * If current > max, return min
 * If current < min, return max
 * @param {number} min
 * @param {number} current
 * @param {number} max
 * @return {number}
 */
export function wrap(min: number, current: number, max: number): number;
