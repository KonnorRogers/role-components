import "../exports/{{ tagWithoutPrefix tag }}/{{ tagWithoutPrefix tag }}-register.js"

suite('<{{ tag }}>', () => {
  test("Should render a component", async () => {
    const el = await fixture(html` <{{ tag }}></{{ tag }}> `);

    expect(el).to.exist;
  })
})
