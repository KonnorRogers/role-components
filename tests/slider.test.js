// import { html, fixture, assert } from "@open-wc/testing"
// import "../exports/components/slider/slider-register.js"

// suite('<role-slider>', () => {
//   test("Should render a component", async () => {
//     const el = await fixture(html` <role-slider></role-slider> `);

//     assert(el.matches(":defined"), `"${el.tagName.toLowerCase()}" element should be ":defined"`)
//   })
// })
